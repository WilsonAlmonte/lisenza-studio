---
import { Icon } from "astro-icon/components";
import site from "../content/site.json";
---

<footer class="flex flex-col gap-10 bg-primary px-[120px] py-[60px] max-lg:px-12 max-md:px-6">
  <!-- Top row -->
  <div class="flex w-full justify-between max-md:flex-col max-md:gap-8">
    <!-- Brand -->
    <div class="flex flex-col gap-3">
      <span class="font-heading text-[22px] font-semibold tracking-[4px] text-white">
        {site.footer.logo}
      </span>
      <span class="font-display text-[13px] italic text-white/55">
        {site.footer.tagline}
      </span>
    </div>

    <!-- Contact links -->
    <div class="flex flex-col items-end gap-3 max-md:items-start">
      <a href={`tel:${site.footer.phone.replace(/[() -]/g, "")}`} class="flex items-center gap-2">
        <Icon name="lucide:phone" class="size-3.5 text-accent" />
        <span class="font-body text-[13px] text-white/75">{site.footer.phone}</span>
      </a>
      <a href={`mailto:${site.footer.email}`} class="flex items-center gap-2">
        <Icon name="lucide:mail" class="size-3.5 text-accent" />
        <span class="font-body text-[13px] text-white/75">{site.footer.email}</span>
      </a>
      <a href="https://instagram.com/lisenza.studio" target="_blank" rel="noopener noreferrer" class="flex items-center gap-2">
        <Icon name="lucide:instagram" class="size-3.5 text-accent" />
        <span class="font-body text-[13px] text-white/75">{site.footer.instagram}</span>
      </a>
    </div>
  </div>

  <!-- Divider -->
  <div class="h-px w-full bg-white/[0.08]"></div>

  <!-- Copyright -->
  <p class="w-full text-center font-body text-xs text-white/35">
    {site.footer.copyright}
  </p>
</footer>
